export const meta = {
  name: 'compa-nemotron-supabase',
  description: '20 agents research Nemotron, design Supabase schema, write generation scripts, produce unified build plan',
  phases: [
    { title: 'Research & Design', detail: '20 specialist agents in parallel: dataset analysis, schema design, script authoring, narrative' },
    { title: 'Synthesize', detail: 'Final synthesizer produces unified implementation document' }
  ]
}

const CONTEXT = '/c/Users/joshb/Documents/Compa/docs/contexto-compa.md'
const ARCH_DOC = '/c/Users/joshb/Documents/Compa/docs/arquitectura-final.md'

const RESEARCH_SCHEMA = {
  type: 'object',
  properties: { findings: { type: 'string', description: 'Complete research findings as markdown' } },
  required: ['findings']
}

const DESIGN_SCHEMA = {
  type: 'object',
  properties: { design: { type: 'string', description: 'Complete design with code' } },
  required: ['design']
}

const SYNTHESIS_SCHEMA = {
  type: 'object',
  properties: {
    plan: { type: 'string' },
    schema: { type: 'string' },
    scripts: { type: 'string' },
    dataSamples: { type: 'string' }
  },
  required: ['plan', 'schema', 'scripts']
}

// ============================================================
// Phase 1: 20 agents in parallel
// ============================================================
phase('Research & Design')

const allResults = await parallel([

  // 1. Nemotron dataset structure
  () => agent('You are a Dataset Archaeologist. Research nvidia/Nemotron-Personas-El-Salvador.\n\nRead ' + CONTEXT + ' first.\n\nResearch the EXACT structure of this HuggingFace dataset:\n1. All columns/fields — exact names and types\n2. The professional_persona text field — format, typical content, length, language\n3. Demographic fields: departamento, municipio, ocupacion, edad, genero\n4. Total records (148k claimed — verify)\n5. The 14 departamentos covered\n6. The 44 municipios covered\n7. The 119 ocupaciones — list at least 30 that are business-related\n8. How names appear in the text (usually first 1-2 sentences)\n9. Any business/entrepreneurship signals already tagged\n10. Dataset accessibility: gated? requires token? size on disk?\n\nSearch the web. Check HuggingFace dataset card. Find sample rows. Give EXACT field names and types for writing Python code against this dataset.', {schema: RESEARCH_SCHEMA, label: 'nemotron-structure'}),

  // 2. Salvadoran MYPE business reality
  () => agent('You are a Salvadoran MYPE Data Specialist.\n\nRead ' + CONTEXT + ' first.\n\nResearch realistic small business profiles in El Salvador:\n1. Top 20 rubros for MYPE — with descriptions\n2. Typical monthly revenue for micro (1-5 emp) and small (6-20 emp) businesses in USD\n3. Business locations beyond San Salvador — rural/semi-urban areas where MYPE operate\n4. Realistic business name patterns in Spanish\n5. Common challenges: credit access, finding clients, government procurement, supply chain\n6. Typical contract sizes a small business can compete for in government procurement\n7. Phone format: +503 XXXX XXXX\n8. DUI format: XXXXXXXX-X, NIT format: XXXX-XXXXXX-XXX-X\n\nUse web search for real data about Salvadoran MYPE sector.', {schema: RESEARCH_SCHEMA, label: 'mype-reality'}),

  // 3. COMPRASAL format research
  () => agent('You are a COMPRASAL Procurement Format Specialist.\n\nRead ' + CONTEXT + ' first.\n\nResearch what real COMPRASAL opportunities look like:\n1. EXACT fields on a COMPRASAL licitacion public page\n2. Real institution names: MINEDUCYT, ANDA, ISSS, MAG, FISDL, MINSAL, MOP, PNC, FOVIAL, CEL, etc.\n3. Contract types: Licitacion Publica, Libre Gestion, Contratacion Directa — what distinguishes each\n4. Realistic contract amounts in USD for each type ($1K-$500K range)\n5. UNSPSC codes commonly used — list top 30\n6. Common rubro categories used in COMPRASAL\n7. URL pattern for COMPRASAL publications\n8. Publication frequency patterns\n\nWe are GENERATING synthetic COMPRASAL data because the real portal is not truly open. Make it realistic.', {schema: RESEARCH_SCHEMA, label: 'comprasal-format'}),

  // 4. Persona text analysis for business signals
  () => agent('You are a Spanish NLP and Persona Text Analyst.\n\nRead ' + CONTEXT + ' first.\n\nAnalyze HOW the Nemotron professional_persona field reveals business ownership. The text is in Salvadoran Spanish.\n\n1. List ALL keywords/phrases signaling business ownership:\n   - "negocio propio", "formalice", "licencia", "matricula de comercio", "microcredito"\n   - "su puesto", "su negocio", "emprendimiento", "comerciante", "dueno de"\n   - "propietario de", "su taller", "ventas", "clientes", "mercado", "local", "tienda"\n   - "ferreteria", "comedor", "pupuseria", "panaderia", "tortilleria", "taller"\n   - Find at least 40 more from web research on Salvadoran business terminology\n2. Name extraction pattern: names usually appear in first 1-2 sentences\n3. Rubro inference: map keywords to standardized business categories\n4. Education/experience level inference\n5. Formal vs informal business signals\n6. Design a scoring function: score(text) -> 0-100 on business_owner_likelihood\n\nSearch for examples of Nemotron persona text in Spanish. Analyze Salvadoran business vocabulary.', {schema: RESEARCH_SCHEMA, label: 'persona-analyzer'}),

  // 5. Supabase architecture
  () => agent('You are a Supabase Architect for n8n integration.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nDesign Supabase setup for Compa:\n1. Free tier limits — can it handle 50-100 rows for demo?\n2. n8n Postgres node vs Supabase REST API node — which for live demo?\n3. Connection: service_role key vs anon key for n8n\n4. Python supabase-py library: exact pip install, connection code, insert patterns\n5. RLS: disable for demo speed? Yes/no with rationale\n6. Project creation step-by-step: create, get URL, get keys, SQL Editor\n7. REST API auto-generation from tables — how ElevenLabs could query productor data\n8. Connection string format for n8n Postgres node\n\nSearch Supabase docs. Provide working code snippets for both Python and n8n.', {schema: RESEARCH_SCHEMA, label: 'supabase-architect'}),

  // 6. Python pipeline design
  () => agent('You are a Python Data Pipeline Engineer.\n\nRead ' + CONTEXT + ' first.\n\nDesign simular_datos_compa.py. The script must:\n1. Load Nemotron dataset from HuggingFace datasets library\n2. Filter for business owners using keyword scoring on professional_persona\n3. Extract names, infer rubros from text\n4. Generate productores_demo.csv and proveedores_demo.csv\n5. Generate synthetic COMPRASAL-style oportunidades\n6. Insert everything into Supabase\n\nProvide:\n1. Exact Python code to load the dataset\n2. The field names returned by the dataset\n3. HuggingFace access requirements (token? gated?)\n4. Keyword scoring algorithm in Python\n5. Rubro inference mapping\n6. Supabase insert code\n7. Required packages: datasets, supabase, pandas, python-dotenv\n8. Fallback: if dataset inaccessible, generate realistic synthetic personas from templates\n\nWrite working code, not pseudocode. Every function must be implemented.', {schema: DESIGN_SCHEMA, label: 'pipeline-engineer'}),

  // 7. n8n + Supabase workflow redesign
  () => agent('You are an n8n + Supabase Integration Specialist.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nRedesign Compa workflows from Google Sheets to Supabase:\n\n1. Workflow 1 (Oportunidades):\n   - Replace Google Sheets Read with Supabase/Postgres query nodes\n   - Query: SELECT * FROM productores WHERE id = demo-prod-001\n   - Query: SELECT * FROM oportunidades\n   - Matching algorithm stays the same (Code node)\n   - HTTP Request to ElevenLabs stays the same\n\n2. Workflow 2 (retar_decision):\n   - Add decision logging: INSERT into decisiones table\n   - Enrich Codex prompt with productor data from Supabase\n\n3. n8n node configuration:\n   - Postgres node: connection string, query, output format\n   - OR HTTP Request to Supabase REST API + anon key\n   - Which is more reliable for a live demo?\n\n4. Environment variables needed in n8n Cloud:\n   - SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY\n   - OPENAI_API_KEY, ELEVENLABS_API_KEY, ELEVENLABS_AGENT_ID\n\nProvide exact node configs and query strings.', {schema: DESIGN_SCHEMA, label: 'n8n-supabase-integration'}),

  // 8. Synthetic data designer
  () => agent('You are a Synthetic Data Designer for Compa demo.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nDesign EVERY row of the demo dataset:\n\nTABLE 1: productores (10-15 rows)\n- col: id, nombre, rubro, ubicacion, capacidad, telefono, dui, nit, created_at\n- 1 primary persona (Vilma or better from Nemotron)\n- 2-3 same-rubro businesses (for competition context)\n- 2-3 different-rubro businesses (should not match same opportunities)\n- 2-3 businesses in different locations\n- Remaining: mix\n\nTABLE 2: oportunidades (20-30 rows)\n- col: id, titulo, institucion, monto, fecha_cierre, rubro_requerido, unspsc_code, url_fuente, tipo_contratacion\n- 5-7 Alimentos y Bebidas opportunities (match Vilma)\n- 3-5 Textiles opportunities\n- 3-5 Construccion opportunities\n- 3-5 Servicios opportunities\n- 5 more mixed\n- Mix of Licitacion Publica, Libre Gestion, Contratacion Directa\n- Prices: $500 to $500,000, most in $5K-$100K range\n\nTABLE 3: proveedores (10-15 rows)\n- Same format as productores\n- Each linked to specific rubros they supply\n- Include distinct personality signals in a "persona_text" field\n\nTABLE 4: decisiones (empty template)\n- col: id, productor_id, decision_descrita, preguntas_json, created_at\n\nDesign every row with realistic values. This data will be INSERTed into Supabase.', {schema: DESIGN_SCHEMA, label: 'data-designer'}),

  // 9. SQL schema author
  () => agent('You are a PostgreSQL Schema Designer.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nWrite the COMPLETE SQL for Supabase. Output a single .sql file:\n\n1. CREATE TABLE productores (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), nombre TEXT NOT NULL, rubro TEXT NOT NULL, ubicacion TEXT, capacidad TEXT, telefono TEXT, dui TEXT, nit TEXT, created_at TIMESTAMPTZ DEFAULT now());\n   Add: indexes on rubro, ubicacion; constraint on telefono format.\n\n2. CREATE TABLE oportunidades (same pattern, with: titulo, institucion, monto DECIMAL, fecha_cierre DATE, rubro_requerido TEXT, unspsc_code TEXT, url_fuente TEXT, tipo_contratacion TEXT)\n   Add: indexes on rubro_requerido, fecha_cierre.\n\n3. CREATE TABLE proveedores (same as productores + persona_text TEXT, rubros_suministra TEXT[])\n\n4. CREATE TABLE decisiones (id UUID, productor_id UUID REFERENCES productores(id), decision_descrita TEXT, preguntas_json JSONB, reasoning_trace TEXT, created_at TIMESTAMPTZ DEFAULT now())\n\n5. INSERT statements for 5 sample rows per table using realistic values from the data designer\n\n6. RLS: DISABLE for demo (ALTER TABLE ... ENABLE ROW LEVEL SECURITY; but create permissive policies)\n\n7. A SQL matching query: SELECT o.*, p.nombre, p.rubro FROM oportunidades o JOIN productores p ON o.rubro_requerido = p.rubro WHERE p.id = :demo_prod_id ORDER BY o.monto DESC;\n\nWrite the SQL ready to paste into Supabase SQL Editor.', {schema: DESIGN_SCHEMA, label: 'schema-designer'}),

  // 10. HuggingFace dataset access
  () => agent('You are a HuggingFace Datasets Specialist.\n\nRead ' + CONTEXT + ' first.\n\nFigure out how to access nvidia/Nemotron-Personas-El-Salvador:\n1. Verify the dataset exists on HuggingFace. Search for it.\n2. Is it gated? Does it require token? Check the dataset card.\n3. If gated: what is the access request process? How long does it take?\n4. If not gated: exact Python code to load it\n5. Dataset size — can we download in 5 minutes on conference WiFi?\n6. Streaming option: dataset = load_dataset(..., streaming=True)\n7. Sample first 5 rows from web/documentation\n8. ALL field names and their types\n9. ALL 14 departamentos (list them)\n10. ALL 44 municipios (list them)\n11. List 30+ ocupaciones that are business-related\n\nIf dataset is NOT accessible within hackathon timeframe, provide:\n- Complete field schema from documentation\n- Python code for a fallback generator that creates 100 realistic persona records matching Nemotron format\n\nSearch HuggingFace, read dataset card, test access if possible.', {schema: RESEARCH_SCHEMA, label: 'hf-dataset-access'}),

  // 11. Transparency narrative
  () => agent('You are a Hackathon Narrative Designer.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nDesign the COMPLETE transparency narrative:\n\nPIVOT: COMPRASAL claims to be "open data" but in practice is NOT accessible or machine-readable. We tried. So we built something MORE transparent: fully synthetic data from nvidia/Nemotron-Personas-El-Salvador — a published, documented, reproducible dataset anchored to the 2024 Salvadoran census. Every persona you see is synthetic but statistically representative of real Salvadoran demographics. All our code is open. All our data generation is reproducible.\n\nDesign:\n1. The 30-second transparency pitch (exact words)\n2. Slide text for the transparency moment\n3. Jury Q&A responses:\n   - "If data is fake, how is this useful?"\n   - "Why not use real COMPRASAL data?"\n   - "How do you know synthetic data is representative?"\n   - "What happens when real COMPRASAL data becomes available?"\n4. Ethical positioning: synthetic data + full transparency > opaque government "open data"\n5. How to show Nemotron on screen during demo\n6. The "trust" angle: we tell you exactly where every data point came from', {schema: RESEARCH_SCHEMA, label: 'transparency-narrative'}),

  // 12. Demo story designer
  () => agent('You are a Demo Story Designer.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nCraft the complete emotional narrative for the Compa demo:\n\n1. The productor: Full backstory. Name, family, business origin, daily struggle, dream. Use Nemotron-inspired details. Make jurors FEEL.\n2. The opportunity: A specific synthetic COMPRASAL opportunity. Why perfect. What at stake.\n3. The decision moment: The genuine dilemma. Not "should I apply" but something deeper.\n4. The 3 questions Compa asks: Write them. Sharp. Specific. Make the jury nod.\n5. The resolution: What happens after the call. The productor next step.\n6. Visual B-roll: Describe 3-4 slides/images accompanying the story.\n\nThis is the emotional core. Technical excellence gets attention. Story wins prizes.', {schema: RESEARCH_SCHEMA, label: 'story-designer'}),

  // 13. ElevenLabs agent redesign for Supabase
  () => agent('You are an ElevenLabs Conversational AI Agent Designer.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nRedesign the ElevenLabs agent for the Supabase architecture:\n\n1. Dynamic variables: How to pull data from Supabase into ElevenLabs outbound calls\n   - Option A: n8n passes dynamic_variables in the POST body\n   - Option B: ElevenLabs webhook queries Supabase directly\n   - Recommend and implement Option A (simpler)\n\n2. System prompt update: Add the transparency angle\n   - If user asks about data source: "Usamos datos del censo salvadoreno 2024, todo sintetico y transparente. A diferencia de otros sistemas, te puedo decir exactamente de donde sale cada numero."\n\n3. Tool definition: retar_decision stays the same. Consider adding a second tool "consultar_perfil" that queries the productor profile from Supabase\n\n4. Conversation flow update: When user expresses doubt about the opportunity, Compa now says "Mira, esta oportunidad la genere basandome en patrones reales de COMPRASAL. No es una oportunidad real especifica, pero es el tipo de cosa que si se publica."\n\n5. Updated tool URL for n8n Cloud webhook\n\nProvide exact agent configuration JSON and updated system prompt.', {schema: DESIGN_SCHEMA, label: 'elevenlabs-redesign'}),

  // 14. Risk and fallback plan for synthetic data
  () => agent('You are a Hackathon Risk Mitigator for data infrastructure.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nDesign the risk mitigation plan for the Nemotron + Supabase stack:\n\n1. Risk: Nemotron dataset inaccessible (gated, too large, no WiFi)\n   Fallback: Pre-written JSON file with 15 realistic personas matching Nemotron format\n\n2. Risk: Supabase project creation fails or times out\n   Fallback: Local JSON files served from the repo; n8n reads files instead of DB\n\n3. Risk: Python script errors during demo prep\n   Fallback: Pre-generated CSVs committed to repo; Python script only needed for "live generation" show\n\n4. Risk: Supabase connection from n8n fails during live demo\n   Fallback: n8n Code node reads data/fallback.json (same pattern as original architecture)\n\n5. Risk: ElevenLabs outbound call API fails\n   Fallback: Pre-recorded MP3s (already in the plan)\n\n6. Risk: Someone asks "where did this data really come from" and the answer is complicated\n   Fallback: Prepared 15-second answer + slide with Nemotron citation\n\nFor each risk: exact mitigation, exact fallback script, exact files needed.', {schema: RESEARCH_SCHEMA, label: 'risk-data-fallbacks'}),

  // 15. Nemotron persona generator (fallback)
  () => agent('You are a Python Synthetic Persona Generator.\n\nRead ' + CONTEXT + ' first.\n\nWrite a FALLBACK Python script (generar_personas_fallback.py) that generates realistic Salvadoran business personas WITHOUT accessing HuggingFace.\n\nThe script must:\n1. Define templates for professional_persona text in Salvadoran Spanish\n2. Randomize: names, ages, departamentos, municipios, ocupaciones, business details\n3. Generate 30 productores and 20 proveedores with:\n   - Realistic Salvadoran names (first + last)\n   - Business rubros with descriptions\n   - Department and municipality from the real 14x44 grid\n   - Realistic phone numbers, DUI, NIT\n   - A 3-5 sentence professional_persona text matching Nemotron format\n4. Output: productores_demo.csv, proveedores_demo.csv\n5. Also output: personas_sinteticas.json with the full persona texts\n\nUse the REAL list of:\n- 14 departamentos: Ahuachapan, Santa Ana, Sonsonate, Chalatenango, La Libertad, San Salvador, Cuscatlan, La Paz, Cabanas, San Vicente, Usulutan, San Miguel, Morazan, La Union\n- 44 municipios (distribute realistically)\n- Common rubros: Alimentos y Bebidas, Textiles y Uniformes, Construccion, Servicios de Limpieza, Transporte, Tecnologia, Mobiliario, Suministros Agricolas, Imprenta, Reciclaje\n\nWrite the COMPLETE runnable Python script. No external APIs needed. Pure Python with random and json modules.', {schema: DESIGN_SCHEMA, label: 'fallback-generator'}),

  // 16. n8n workflow JSON exporter
  () => agent('You are an n8n Workflow JSON Specialist.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nProduce the n8n workflow JSON export code for both Compa workflows using Supabase:\n\nWORKFLOW 1: Oportunidades (Supabase)\n- Manual Trigger node\n- Cron Trigger node (every 6 hours)\n- Postgres node: SELECT * FROM productores\n- Postgres node: SELECT * FROM oportunidades\n- Code node: matching algorithm (JavaScript)\n- IF node: score >= 60\n- Set node: build ElevenLabs payload\n- HTTP Request node: POST to ElevenLabs\n- Error handling: Code node for fallback\n\nWORKFLOW 2: retar_decision (Supabase)\n- Webhook node: POST /retar-decision\n- Code node: validate input\n- HTTP Request node: POST to OpenAI (gpt-4o-mini)\n- Code node: parse response\n- Postgres node: INSERT into decisiones\n- Respond to Webhook node\n- Error branch: Code node + Respond with fallback\n\nFor each workflow, provide:\n1. The exact node type names as they appear in n8n\n2. The key configuration for each node (query, URL, headers, body)\n3. The JavaScript code for each Code/Function node\n4. The connection between nodes (which node connects to which)\n\nNot the full n8n JSON export format — just the logical node sequence with exact configs and code.', {schema: DESIGN_SCHEMA, label: 'n8n-json-exporter'}),

  // 17. Supabase setup guide author
  () => agent('You are a Technical Documentation Writer.\n\nRead ' + CONTEXT + ' first.\n\nWrite the SUPABASE_SETUP.md guide:\n\n1. Create Supabase account (app.supabase.com)\n2. Create new project: name "compa", region "US East", password (generate)\n3. Wait for project to provision (~2 minutes)\n4. Get credentials: Project URL, anon key, service_role key\n5. Open SQL Editor\n6. Paste and run schema.sql (the CREATE TABLE statements)\n7. Paste and run seed.sql (the INSERT statements)\n8. Verify: SELECT COUNT(*) FROM productores, oportunidades, proveedores\n9. Get connection string for n8n: postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres\n10. Configure n8n Postgres node with connection string\n11. Test: run a simple SELECT query from n8n\n\nInclude screenshots descriptions (what each screen looks like).\nInclude common errors and fixes:\n- "Password reset" — where to find it\n- "Connection refused" — check IP allowlist or use connection pooling\n- "Relation does not exist" — run schema.sql first\n\nThis guide should take someone who has NEVER used Supabase from zero to working Compa connection in 15 minutes.', {schema: DESIGN_SCHEMA, label: 'supabase-setup-guide'}),

  // 18. COMPRASAL opportunity template generator
  () => agent('You are a COMPRASAL Data Template Designer.\n\nRead ' + CONTEXT + ' first.\n\nDesign a Python function that generates realistic COMPRASAL-style opportunities.\n\nThe function generate_oportunidades(n=25) must produce JSON with:\n\nFor each opportunity:\n{\n  "id": "opp-NNN",\n  "titulo": "Servicio de [COSA] - [UBICACION/LOTE] - [INSTITUCION]",\n  "institucion": one of the 20 real Salvadoran government institutions,\n  "monto": realistic USD amount (weighted toward small business range),\n  "fecha_cierre": date within next 30 days,\n  "rubro_requerido": one of the standardized rubros,\n  "unspsc_code": matching UNSPSC code for the rubro,\n  "url_fuente": realistic fake COMPRASAL URL,\n  "tipo_contratacion": "Licitacion Publica" | "Libre Gestion" | "Contratacion Directa",\n  "descripcion": 2-3 sentence description in formal Spanish procurement language\n}\n\nInclude:\n1. The 20 real government institutions with their acronyms\n2. The rubro-to-UNSPSC mapping\n3. Contract type distribution: 30% LP, 50% LG, 20% CD\n4. Amount distribution: 60% under $50K, 30% $50K-$200K, 10% $200K-$500K\n5. Titulo templates: 10+ patterns with placeholders\n6. Realistic procurement descriptions in formal Spanish\n\nWrite the COMPLETE Python function. Use random module. Make it produce believable government procurement data.', {schema: DESIGN_SCHEMA, label: 'comprasal-generator'}),

  // 19. Integration test plan
  () => agent('You are a QA Engineer for hackathon projects.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nDesign the integration test plan for Compa:\n\n1. Test 1: Supabase connection — n8n Postgres node can query productores\n2. Test 2: Matching algorithm — given Vilma + 25 oportunidades, verify 5+ matches returned\n3. Test 3: ElevenLabs outbound call — POST to API returns 200 (don\'t need real call, just valid request)\n4. Test 4: retar_decision webhook — POST valid decision_descrita, verify 3 preguntas returned within 5 seconds\n5. Test 5: retar_decision fallback — POST invalid input, verify fallback preguntas returned\n6. Test 6: Codex response parsing — test with mocked OpenAI response, verify JSON parse works\n7. Test 7: Decision logging — verify INSERT into decisiones table works\n8. Test 8: E2E — manual trigger Workflow 1, verify it queries Supabase, runs matching, builds payload, calls ElevenLabs\n9. Test 9: E2E — POST to retar_decision, verify Supabase query, Codex call, decision logged\n10. Test 10: n8n offline — disconnect internet, verify fallback.json is read\n\nFor each test: exact curl command or n8n manual trigger step, expected output, pass/fail criteria.\n\nAlso: a 1-page test checklist for the 30-minute integration window.', {schema: DESIGN_SCHEMA, label: 'integration-tests'}),

  // 20. Final architecture update
  () => agent('You are a Technical Architect updating the Compa architecture document.\n\nRead ' + CONTEXT + ' and ' + ARCH_DOC + ' first.\n\nWrite the updated architecture sections reflecting the Nemotron + Supabase pivot:\n\n1. Why the pivot: COMPRASAL claims open data but is not accessible. Nemotron is published, documented, reproducible, and anchored to census 2024. Supabase replaces Google Sheets for a real database that can grow beyond the hackathon.\n\n2. Updated stack:\n   - Data generation: nvidia/Nemotron-Personas-El-Salvador (primary) + fallback generator script\n   - Database: Supabase (PostgreSQL), free tier\n   - Orchestration: n8n Cloud (unchanged)\n   - Voice: ElevenLabs Conversational AI (unchanged)\n   - Reasoning: Codex/OpenAI gpt-4o-mini (unchanged)\n\n3. Updated project structure:\n   - Add: supabase/schema.sql, supabase/seed.sql\n   - Add: scripts/simular_datos_compa.py, scripts/generar_personas_fallback.py\n   - Add: scripts/generar_oportunidades.py\n   - Modify: n8n-exports/ (now uses Postgres nodes)\n   - Keep: data/fallback.json (now as Supabase fallback)\n\n4. Updated build order (reflects Supabase setup before n8n)\n\n5. Updated matching algorithm (same logic, now queries Postgres not Sheets)\n\n6. Updated environment variables\n\n7. Transparency architecture: how we show data provenance in the demo\n\nWrite these as replacement sections for docs/arquitectura-final.md. Mark clearly what changed and why.', {schema: DESIGN_SCHEMA, label: 'architecture-update', model: 'opus'})
])

// ============================================================
// Phase 2: Synthesize
// ============================================================
phase('Synthesize')

const allContext = allResults
  .filter(Boolean)
  .map(function(r) { return r.findings || r.design || '' })
  .join('\n---\n')

const finalOutput = await agent('You are the Final Build Plan Synthesizer for Compa.\n\nALL RESEARCH AND DESIGNS FROM 20 SPECIALIST AGENTS:\n' + allContext.slice(0, 18000) + '\n\n---\n\nProduce the definitive implementation document. Must contain these EXACT sections:\n\n# Compa — Implementacion Nemotron + Supabase\n\n## 1. Stack Final\n- Exact technologies, versions, plans. No options. No "or".\n\n## 2. Supabase Schema (SQL completo)\n- Every CREATE TABLE statement\n- Every INSERT statement for demo data (at least 5 rows per table)\n- Indexes and RLS policies\n\n## 3. Python Scripts\n- simular_datos_compa.py: complete, runnable code\n- generar_personas_fallback.py: complete, runnable code (no external APIs)\n- generar_oportunidades.py: complete, runnable code\n\n## 4. n8n Workflows (Supabase version)\n- Workflow 1: exact node sequence, configs, code\n- Workflow 2: exact node sequence, configs, code\n- Environment variables list\n\n## 5. Supabase Setup Guide\n- Step by step from zero to working connection\n\n## 6. Demo Data Summary\n- Primary productor persona\n- Top 5 matching opportunities\n- Expected matching results\n\n## 7. Transparency Narrative\n- The 30-second pitch about synthetic data\n- Jury Q&A responses\n\n## 8. Risk Fallbacks\n- What fails, exact mitigation, exact files needed\n\n## 9. Build Order\n- Exact sequence with time estimates for 3 people\n\n## 10. Integration Test Checklist\n- Every test with pass/fail criteria\n\nFormat as clean markdown. Write real code. No hand-waving.', {schema: SYNTHESIS_SCHEMA, label: 'final-synthesizer', model: 'opus'})

return {
  agentsCompleted: allResults.filter(Boolean).length,
  implementationPlan: finalOutput.plan,
  sqlSchema: finalOutput.schema,
  pythonScripts: finalOutput.scripts,
  dataSamples: finalOutput.dataSamples
}